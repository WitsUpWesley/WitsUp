<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.witsup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ViewAnswers"></activity>
        <activity android:name=".ViewQuestions" />
        <activity android:name=".QandAPage" />
        <activity android:name=".AnnouncementPage" />
        <activity android:name=".LogInPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".CreateCourse" />
        <activity android:name=".CourseDetails" />
        <activity android:name=".Enrolincourse" />
        <activity android:name=".Resourcesforfb" />
        <activity android:name=".ViewUploadedFiles" />
        <activity android:name=".testListActivity" />
    </application>

</manifest>